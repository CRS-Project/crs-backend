meta {
  name: Get All
  type: http
  seq: 2
}

get {
  url: {{host}}/api/v1/area-of-concern-group?page&take&sort&sort_by&filter=o&filter_by=search
  body: none
  auth: bearer
}

params:query {
  page: 
  take: 
  sort: 
  sort_by: 
  filter: o
  filter_by: search
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
