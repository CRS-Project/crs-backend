meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{host}}/api/v1/package/
  body: json
  auth: inherit
}

body:json {
  {
    "Name": "Bahlul"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
